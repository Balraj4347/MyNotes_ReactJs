import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#005ce2",
    },
    secondary: {
      main: "#80d19d",
    },
    error: {
      main: "#d10b04",
    },
    side: {
      main: "#8ded26",
    },
  },
});
export default theme;
